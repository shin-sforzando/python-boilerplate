image:
  file: .gitpod.Dockerfile

tasks:
  - init: |
      eval $(gp env -e)
  - command: |
      eval $(gp env -e)
      make

ports:
  - port: 8000
    visibility: private
    onOpen: notify

vscode:
  extensions:
    - ms-azuretools.vscode-docker
